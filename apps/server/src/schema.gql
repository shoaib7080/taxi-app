# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  token: String!
  user: User!
}

input CreateRideInput {
  destLat: Float!
  destLng: Float!
  originLat: Float!
  originLng: Float!
  price: Float!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

enum DriverApprovalStatus {
  APPROVED
  PENDING
  REJECTED
}

type DriverDetails {
  approvalStatus: DriverApprovalStatus!
  id: ID!
  isOnline: Boolean!
  licenseNumber: String!
  rating: Float!
  user: User!
  vehicleColor: String
  vehiclePlate: String!
  vehicleType: String!
}

type Mutation {
  acceptRide(driverId: String!, rideId: String!): Ride!
  bookInstantRide(createRideInput: CreateRideInput!): Ride!
  bookSmartSaverRide(offerId: String!): Ride!
  createDriver(email: String!, fullName: String!, password: String!): AuthResponse!
  createUser(fullName: String!, phone: String!): User!
  login(appType: String!, email: String!, password: String!, pushToken: String): AuthResponse!
  signup(email: String!, fullName: String!, password: String!): AuthResponse!
  startRide(driverId: String!, otp: String!, rideId: String!): RideOffer!
}

type Query {
  findSmartSaverRides(destLat: Float!, destLng: Float!, pickupLat: Float!, pickupLng: Float!): [RideOffer!]!
  getRide(id: String!): Ride!
  getUsers: [User!]!
}

type Ride {
  createdAt: DateTime!
  destLat: Float!
  destLng: Float!
  driver: User
  generatedOffer: RideOffer
  id: ID!
  originLat: Float!
  originLng: Float!
  price: Float!
  rideOtp: String
  status: RideStatus!
  user: User!
}

type RideOffer {
  availableTime: DateTime!
  destLat: Float!
  destLng: Float!
  id: ID!
  originLat: Float!
  originLng: Float!
  price: Float!
  status: RideOfferStatus!
}

enum RideOfferStatus {
  AVAILABLE
  BOOKED
  EXPIRED
}

enum RideStatus {
  ACCEPTED
  CANCELLED
  COMPLETED
  CONFIRMED
  SEARCHING
}

type User {
  createdAt: DateTime!
  driverDetails: DriverDetails
  email: String!
  fullName: String!
  googleId: String
  id: ID!
  isPhoneVerified: Boolean!
  phone: String
  pushToken: String
  rides: [Ride!]
  roles: [String!]!
}